for(初始化表达式; 循环条件表达式; 循环后的操作表达式)
{
	执行语句;
}

注意：
	1、循环条件表达式必须要有真假；
	2、初始化表达式只要满足表达式格式就行；
	3、当多表达式的时候用逗号分隔开；
	4、for循环中，初始化表达式和循环后的操作表达式都可以不写；
int y = 0;
for( ; y<3 ; )
{
      y++;
}

无限循环的最简单表现形式。
for(；；){}

while(true){}